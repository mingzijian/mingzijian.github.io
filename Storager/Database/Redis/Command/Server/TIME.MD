TIME (当前Redis所在服务器时间)
-----------------------------------------------

### 参数说明
无参数

### 可用版本
2.6.0及以上

### 时间复杂度
O(1)

### 返回值
一个包含两个字符串的列表：
- 当前时间(以 UNIX 时间戳格式表示) 
	UNIX 时间戳是从1970年01月01日（UTC/GMT 00:00:00）开始所经过的秒数，不考虑闰秒。
- 当前这一秒钟已经逝去的微秒数
Redis-版本说明
-----------------------
### 命名规则
Redis借鉴了Linux操作系统对于版本号的命名规则：

<br/>
<pre>版本号第二位如果是奇数，则为非稳定版本（例如2.7、2.9、3.1），如果是偶数，则为稳定版本（例如2.6、2.8、3.0、3.2），

当前奇数版本就是下一个稳定版本的开发版本，例如2.9版本是3.0版本的开发版本
</pre>
